[%

  var util := new Native("org.tura.metamodel.commons.Util");
  
  if (mapper.getQueries().size()<> 0) {
    var query = mapper.getQueries().get(0);
    var type = util.runQuery(query,mapper);
 }
  

%]